# Client-Server

Projeto para reproduzir um cliente e servidor onde o servidor faz uma requisição para uma API externa e retorna o dado solicitado pelo cliente.

## Como rodar

### Rodando o servidor

Abra um terminal, navegue até a pasta `/server` e execute: go run .


### Rodando o cliente

Abra outro terminal, com o servidor rodando, navegue até a pasta `/client` e execute: go run .


## Observações

- Certifique-se de ter o [Go](https://golang.org/dl/) instalado.
- Execute o servidor antes do cliente.
